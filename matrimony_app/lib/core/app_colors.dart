import 'package:flutter/material.dart';

/// A class that holds the color scheme used throughout the HeartLink.net app.
class AppColors {
  // ---------------------------------------------------------------------------
  // Primary Theme Colors
  // ---------------------------------------------------------------------------

  /// The primary color of the app.
  static const Color primary = Color(0xFFE91E63);

  /// The secondary color of the app.
  static const Color secondary = Color(0xFF519ACF);

  // ---------------------------------------------------------------------------
  // Light Theme Colors
  // ---------------------------------------------------------------------------

  /// The background color used in the light theme.
  static const Color lightBackground = Color(0xFFE0E0E0);

  /// The text color used in the light theme.
  static const Color lightText = Colors.black;

  // ---------------------------------------------------------------------------
  // Dark Theme Colors
  // ---------------------------------------------------------------------------

  /// The background color used in the dark theme.
  static const Color darkBackground = Color(0xFF1E1E1E);

  /// The text color used in the dark theme.
  static const Color darkText = Colors.white;
}
